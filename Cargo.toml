[package]
name = "scl-90"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = "0.24.1"  # egui framework
csv = "1.3.0"      # for reading CSV file
serde = { version = "1.0", features = ["derive"] }  # for data serialization
ron = "0.8"        # for RON file format
