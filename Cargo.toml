[package]
name = "scl-90"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = "0.31.0"  # egui framework
csv = "1.3.1"      # for reading CSV file
serde = { version = "1.0.218", features = ["derive"] }  # for data serialization
ron = "0.8.1"        # for RON file format
